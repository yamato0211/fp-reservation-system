<nav>
  <%= link_to 'ホーム', users_path %>
  <%= link_to 'マイページ', edit_user_registration_path %>
  <%= link_to t("ログアウト"), destroy_user_session_path, data: { turbo_method: :delete } %>
</nav>

<div class="container">
  <div class="row">
    <div class="col-12 text-center">
      <h1>登録画面</h1>
      <p>3ヶ月先まで登録することができます。</p>
    </div>
    <div class="col-12 mt-3">
      <%= render SimpleCalendar::UserWeekCalendar.new(self) do |date| %>
        <%= date.day %>
      <% end %>
    </div>
  </div>
</div>